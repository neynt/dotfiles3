#!/bin/bash
if [ "$#" -gt 1 ]; then
  echo "Usage: $0 [FILE]"
  echo "If FILE not defined, reads from stdin."
  exit
fi
if [ "$#" -eq 0 ]; then
  curl -F "file=@-" https://uploadere.neynt.ca/upload
  echo
else
  curl -F "file=@$1" https://uploadere.neynt.ca/upload
  echo
fi
